export const CMD_PREFIX = 'gitlabSnippets.';

export enum VISIBILITY {
  private = 'private',
  internal = 'internal',
  public = 'public',
}

export enum SNIPPET_GROUP {
  category = 0,
  project = 1,
}